# ── 비밀·자격증명 안전망 ─────────────────────────────
# 이 이름 패턴에 걸리는 파일은 git이 아예 없는 셈 치고 무시한다.
# (git add . 을 해도 올라가지 않음. 단, 파일 '내용'까지 검사하지는
#  못하므로 푸시 전 내용 확인은 별도로 한다.)
.env
.env.*
*.env
*.env.*
*.key
*.pem
*secret*
*password*
*.bak

# ── 시스템 잡동사니 ─────────────────────────────────
.DS_Store
Thumbs.db
